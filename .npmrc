# electron and esbuild ship platform binaries via postinstall; without this pnpm
# skips those scripts and the app has no runtime to launch.
only-built-dependencies[]=electron
only-built-dependencies[]=esbuild
only-built-dependencies[]=@tailwindcss/oxide
